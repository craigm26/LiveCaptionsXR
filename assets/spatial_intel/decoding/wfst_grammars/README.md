# Placeholder WFST Grammars

Add compiled WFST grammars (numbers, dates, commands) in this directory. The predictive caption engine reads the paths defined in `policies.yaml` to constrain deterministic decoding.

