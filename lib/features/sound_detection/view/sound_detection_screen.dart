// Placeholder for SoundDetectionScreen
import 'package:flutter/material.dart';

class SoundDetectionScreen extends StatelessWidget {
  const SoundDetectionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sound Detection')),
      body: const Center(child: Text('Sound Detection Placeholder')),
    );
  }
} 